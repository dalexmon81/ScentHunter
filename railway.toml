[build]
builder = "RAILPACK"
buildCommand = "python -m pip install -r backend/requirements.txt && python -m playwright install --with-deps chromium"

[deploy]
startCommand = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
